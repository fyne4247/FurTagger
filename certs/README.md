# Local signing materials (gitignored)

Put private signing assets **here only** — this whole directory is in `.gitignore`.

Examples (never commit):
- `Developer ID Application` export (`.p12`)
- App Store Connect API key (`AuthKey_XXXX.p8`)
- Notarization profile notes

Build signing uses these from your machine / Keychain; see `packaging/README.md`.
